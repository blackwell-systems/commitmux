[package]
name = "commitmux-mcp"
version = "0.1.0"
edition = "2021"

[dependencies]
commitmux-types = { path = "../types" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
anyhow = "1"
